apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "nginx.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
